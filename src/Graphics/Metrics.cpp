#include <FRONTIER/Graphics/Metrics.hpp>

namespace fg
{
	/////////////////////////////////////////////////////////////
	Metrics::Metrics(int maxH,int minH,int lineGap) : maxH(maxH),
													  minH(minH),
													  lineGap(lineGap)
	{
		
	}
}
